# Runtime dependencies EXCEPT PyTorch.
# Inside the NVIDIA NGC PyTorch container, torch/numpy/scipy are already
# installed and CUDA-enabled: do NOT reinstall torch there.
# Outside Docker, install PyTorch first (https://pytorch.org/get-started), then:
#   pip install -r requirements.txt && pip install -e .
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
pyriemann>=0.6
mne>=1.5
matplotlib>=3.7
pyyaml>=6.0
pytest>=7

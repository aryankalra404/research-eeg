# Core
numpy>=1.24
scipy>=1.10
pandas>=2.0

# EEG processing
mne>=1.5

# ML / DL
scikit-learn>=1.3
torch>=2.1          # on Mac: pip installs MPS-enabled build by default (arm64)
                     # on Windows w/ NVIDIA GPU: install CUDA build per pytorch.org

# Signal processing extras
PyWavelets>=1.4      # if you use wavelet-based artifact/denoising later

# Visualization / analysis
matplotlib>=3.7
seaborn>=0.12
umap-learn>=0.5       # for GAN validation (real vs synthetic distribution overlap)

# Utilities
tqdm>=4.66
